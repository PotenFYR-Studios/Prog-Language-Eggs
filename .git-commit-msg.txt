fix(docker): switch base to debian:bookworm-slim to fix arm/v7 libc-bin failure

Ubuntu jammy's armhf libc-bin (ldconfig) dpkg trigger crashes under QEMU
emulation on linux/arm/v7, failing every multi-arch build at the first apt
layer with "Errors were encountered while processing: libc-bin".

Debian bookworm's armhf packages configure cleanly under current binfmt/QEMU.
All pinned packages verified present in bookworm via live container dry-run
(43 packages, zero missing). Bonus: slimmer base image, faster pulls.

Also removes the temporary package-verification helper script.
