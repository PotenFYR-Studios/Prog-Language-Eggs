feat: single image + single egg rebrand to Multi, run on any host architecture and OS

Single image / single egg completion
- egg-programming-universal.json renamed to egg-programming-multi.json (git mv)
- docker_images collapsed from 11 variant tags to ONE: :latest
- Dockerfile flattened: all RUNTIME_VARIANT conditionals removed; every runtime
  installs unconditionally with properly guarded optional components (bun/deno/uv
  now skip cleanly on unsupported CPUs instead of breaking the build)
- CI reduced from 11-job matrix to a single build publishing :latest (+ SHA tag)

Universal -> Multi rebrand (no breaking changes)
- All scripts, banners, comments, README and test suites rebranded
- Startup command, variable names, config keys unchanged = zero panel impact

Any architecture where the panel is hosted
- install-runtime.sh: tolerant arch detection (amd64/arm64/armv7/ppc64le/s390x/
  riscv64/unknown) with per-engine availability checks + actionable messages;
  node dist tokens fixed for armv7l/ppc64le/s390x; guards added to all 9 engines
- CI publishes linux/amd64 + linux/arm64 + linux/arm/v7
- Dockerfile optional components degrade gracefully per CPU

Any OS / distro inside the container
- entrypoint ensure_core_tools(): detects alpine/debian/fedora/suse bases and
  auto-installs missing curl/jq/xz/unzip/tar when root, precise admin guidance
  when non-root; architecture shown in boot banner card
